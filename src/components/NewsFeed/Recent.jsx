import React from "react"

const Recent = ({ data }) => {
    return (
        <div>
            {/* {data.map((item, index) => (
                    <div key={item.id}>
                        <h1>{item.title}</h1>
                    </div>
                ))} */}
        </div>
    )
}

export default Recent
